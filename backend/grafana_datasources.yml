apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: 15s
      prometheusVersion: "2.0"
    version: 1
  
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    database: customer_intelligence
    username: postgres
    secureJsonData:
      password: sushanth123
    editable: true
    version: 1
